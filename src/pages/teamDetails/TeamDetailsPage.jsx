import { useEffect } from "react";
import "./TeamDetailsPage.css";

const TeamDetails = ({ match }) => {
  // get team details using id in match params
  useEffect(() => {}, []);

  return <div className="details--container">teams details</div>;
};

export default TeamDetails;
